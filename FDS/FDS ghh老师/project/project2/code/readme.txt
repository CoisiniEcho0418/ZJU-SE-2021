Tips1: 请保证每次输入的测试用例中不包含绝对值相同的整数，否则程序可能会报错（因为本实验没有规定如果结点的key相同时往左插还是往右插，所以当先后插入两个key值相同的节点时，会被认定为无效输入，本程序暂未针对无效输入进行处理，程序会直接异常退出）。

Tips2: 在绝大多数情况下，程序能够正常编译并且运行成功。如果实在编译不了，可以把此目录下的 "project2.exee" 文件后缀名改为exe，这样就可以获得一个可直接运行的exe文件。

Tips3：一次性输入多个测试用例时，最后还需要按下Enter键，程序才会输出全部的测试结果。